Basic. GTK/Webkit, interface for Oauth2

Usage:
    yuri = 'https://some_service.oauth.url'
    def callback (arg):# arg is code for oauth2
        print 'callbacked: ', arg
        return arg
    oauthGui = OauthGui(yuri, callback)
    gtk.main()
    
Know Issues:
	1 - ssl error when use yql after webkit browser
	2 - wx.html.HtmlWindow cannot open https
	
	This two bugs make this module useless :(
